export interface WildseaTrackData {
  name: string;
  visible: boolean;
  length: number;
  progress: number;
  burn: number;
}

export interface WildseaTracklistData {
  name: string;
  tracks: string[];
}
